<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <record id="planning_reminder_sms_template" model="sms.template">
      <field name="name">ARGOS - Appointment reminder</field>
      <field name="model_id" ref="planning.model_planning_slot"/>
      <field name="lang">${object.partner_id.lang}</field>
      <field name="body">Reminder of your appointment with ${object.employee_id.name} for ${object.patient_id.name} at ${object.start_datetime} at your ${object.operating_unit_id.name}.
If you can't, please call us at ${object.operating_unit_id.phone}
      </field>
    </record>
  </data>
</odoo>