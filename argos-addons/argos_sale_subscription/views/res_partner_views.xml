<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

    <record id="action_open_subscriptions" model="ir.actions.act_window">
      <field name="name">Subscriptions</field>
      <field name="context">{'default_patient_id': active_id}</field>
      <field name="domain">[('patient_id', '=', active_id)]</field>
      <field name="res_model">sale.subscription</field>
    </record>

    <record id="res_partner_view_form_inherit" model="ir.ui.view">
      <field name="name">res.partner.view.form.inherit</field>
      <field name="model">res.partner</field>
      <field name="inherit_id" ref="argos_contact.res_partner_view_form"/>
      <field name="arch" type="xml">
        <div name="button_box" position="inside">
          <button class="oe_stat_button" name="%(argos_sale_subscription.action_open_subscriptions)d" icon="fa-refresh"
                  type="action">
            <field name="patient_subscription_count" widget="statinfo" string="Subscriptions"/>
          </button>
        </div>
      </field>
    </record>

  </data>
</odoo>