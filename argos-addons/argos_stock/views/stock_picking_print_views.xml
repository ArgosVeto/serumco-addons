<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <record id="stock_picking_print_view_form" model="ir.ui.view">
      <field name="name">stock.picking.print.view.form</field>
      <field name="model">stock.picking.print</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <group col="4">
              <newline/>
              <field name="move_line_ids" nolabel="1" attrs="{'invisible': [('move_line_ids', '=', [])]}">
                <tree editable="bottom" create="0" delete="0">
                  <field name="company_id" invisible="1"/>
                  <field name="date"/>
                  <field name="product_id"/>
                  <field name="initial_qty"/>
                  <field name="delivered_qty"/>
                  <field name="qty_done" string="To deliver"/>
                  <field name="remaining_qty"/>
                </tree>
              </field>
            </group>
            <group string="No Available Quantity" attrs="{'invisible': [('move_line_ids', '!=', [])]}"/>
            <newline/>
            <separator/>
          </sheet>
          <footer>
            <button name="button_validate" string="Print" type="object" class="btn-primary"
                    attrs="{'invisible': [('move_line_ids', '=', [])]}"/>
            <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>

    <record model="ir.actions.act_window" id="action_print_picking">
      <field name="name">Print Picking</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">stock.picking.print</field>
      <field name="view_mode">form</field>
      <field name="target">new</field>
    </record>
  </data>
</odoo>